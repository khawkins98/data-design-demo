{
  "candidate": "Ant Design",
  "host": "mangrove",
  "versions": {
    "library": "antd@6.5.3, @ant-design/cssinjs@2.1.2, dayjs@1.11.21",
    "react": "19.2.8",
    "node": "22 (CI), 24.18.0 (local run)"
  },
  "requirements": [
    {
      "id": "buttons",
      "status": "native",
      "notes": "Button with type=primary, default, danger and disabled. antd does not uppercase labels, so unlike MUI no textTransform override was needed for the German and Arabic fixtures.",
      "customLinesOfCode": 0
    },
    {
      "id": "links",
      "status": "native",
      "notes": "Typography.Link. On this host Mangrove's own base `a` rule applies to inline links in body copy, which is the WCAG 1.4.1 finding already tracked against Mangrove (_foundational.scss:137) rather than an antd defect. antd's link colour is overridden by the host because antd's rules are layered and Mangrove's are not.",
      "customLinesOfCode": 0
    },
    {
      "id": "text-input",
      "status": "native",
      "notes": "Input inside Form.Item. THE CONTROL DOES NOT LOOK LIKE ANTD ON THIS HOST. Mangrove styles bare input[type=text] unlayered, and antd's styles are inside a CSS @layer, so Mangrove wins regardless of specificity. Measured: 2px #1a1a1a border, 46px height, 0 radius, Roboto - byte-identical to a bare Mangrove input. The controlHeight and borderRadius seed tokens do not reach it. Whether that is the desired outcome is a decision, not a bug: see humanReviewRequired.",
      "customLinesOfCode": 0
    },
    {
      "id": "validation-states",
      "status": "native",
      "notes": "Form.Item validateStatus plus help. antd is the only candidate in this evaluation with a real form-level error channel, so server-rejected is a library affordance rather than component state we invented: see form-states.",
      "customLinesOfCode": 0
    },
    {
      "id": "disabled-states",
      "status": "native",
      "notes": "disabled on Button and Input. Contrast of the disabled text is antd's derived colorTextDisabled and passed axe.",
      "customLinesOfCode": 0
    },
    {
      "id": "select-small",
      "status": "native",
      "notes": "Select with options. 8 items.",
      "customLinesOfCode": 0
    },
    {
      "id": "select-medium",
      "status": "native",
      "notes": "Same component, 40 items. listHeight caps the dropdown.",
      "customLinesOfCode": 0
    },
    {
      "id": "select-large",
      "status": "native",
      "notes": "Same component, 400 items. antd virtualises its option list BY DEFAULT, so unlike the MUI run there is no filterOptions limiting and unlike React Aria there is no virtualisation to add.",
      "customLinesOfCode": 0
    },
    {
      "id": "multiselect",
      "status": "native",
      "notes": "mode=\"multiple\" on the same Select. A prop, not a composition, and it renders removable tags.",
      "customLinesOfCode": 0
    },
    {
      "id": "combobox-searchable",
      "status": "native",
      "notes": "showSearch on the same Select. TRAP: optionFilterProp=\"label\" is required, because antd filters on value by default and the fixture values are slugs while the labels carry the translated text. Without it a searchable select over these fixtures silently matches almost nothing.",
      "customLinesOfCode": 0
    },
    {
      "id": "date-picker",
      "status": "native",
      "notes": "DatePicker on dayjs, fed only from TODAY_ISO.",
      "customLinesOfCode": 0
    },
    {
      "id": "datetime-range-picker",
      "status": "native",
      "notes": "THE CLEAREST DIFFERENCE IN THE EVALUATION. DatePicker.RangePicker with showTime is one component in the free package, verified against antd 6.5.3's own types. MUI needed two pickers plus our own minDateTime cross-reference because the real range picker is behind @mui/x-date-pickers-pro; React Aria and Carbon composed it too. The ordering guarantee here belongs to the component, so there is no application code to maintain. Asserted in e2e by counting .ant-picker-range, not inputs: a RangePicker renders two inputs and an input count would read identically to the composed version.",
      "customLinesOfCode": 0
    },
    {
      "id": "modal",
      "status": "native",
      "notes": "Modal with focus trap and Escape. Rendered inside the candidate subtree via ConfigProvider getPopupContainer rather than at document.body, so the UNDRR custom properties resolve.",
      "customLinesOfCode": 0
    },
    {
      "id": "tooltip",
      "status": "native",
      "notes": "Tooltip. Same container treatment as the modal.",
      "customLinesOfCode": 0
    },
    {
      "id": "popover",
      "status": "native",
      "notes": "Popover with trigger=\"click\". One getPopupContainer on the provider covers all three overlay types, where the React Aria and Mantine runs needed a token-scope class passed to each overlay individually.",
      "customLinesOfCode": 0
    },
    {
      "id": "accordion",
      "status": "native",
      "notes": "Collapse with accordion, driven by the items prop.",
      "customLinesOfCode": 0
    },
    {
      "id": "cards",
      "status": "native",
      "notes": "Card variant=\"outlined\". Matching Mangrove's mg-card is easier than it was for MUI because antd's card is already flat and square-ish. The project tokens now carry Mangrove's current UNDRR interactive palette, but Mangrove 1.8.1 exposes no runtime tokens, so antd consumes a separately maintained mapping rather than inheriting future changes.",
      "customLinesOfCode": 0
    },
    {
      "id": "left-nav",
      "status": "native",
      "notes": "Menu mode=\"inline\". antd's most opinionated component here: it brings its own selected-item highlight, indent scale and hover transition. Those are theme tokens rather than CSS overrides, so they are reachable, but the derived selected-item colour failed contrast and had to be pinned - see theming.",
      "customLinesOfCode": 0
    },
    {
      "id": "table-render",
      "status": "native",
      "notes": "Table over all 250 fixture rows. Numbers and dates are formatted with Intl against the fixture locale rather than by antd.",
      "customLinesOfCode": 0
    },
    {
      "id": "table-sort",
      "status": "native",
      "notes": "sorter per column, with defaultSortOrder on eventDate.",
      "customLinesOfCode": 0
    },
    {
      "id": "table-multiselect",
      "status": "native",
      "notes": "rowSelection, and the select-all header checkbox is present BY DEFAULT - hideSelectAll exists to remove it. Verified in the DOM rather than assumed, because the React Aria run recorded this as native while rendering zero checkboxes and I published that before it was caught.",
      "customLinesOfCode": 0
    },
    {
      "id": "table-filter",
      "status": "native",
      "notes": "filters plus onFilter on the hazard column, with filterSearch for the value list.",
      "customLinesOfCode": 0
    },
    {
      "id": "table-paginate",
      "status": "native",
      "notes": "pagination with showSizeChanger and pageSizeOptions. showTotal is set so the evidence run can assert the 250-row total.",
      "customLinesOfCode": 0
    },
    {
      "id": "table-column-resize-or-reorder",
      "status": "custom",
      "notes": "NOT unsupported, but antd has no column sizing of any kind: there is no resizable on ColumnType and nothing in @rc-component/table provides it - the resize matches in that package are ResizeObserver for sticky headers and cell measurement. antd's own documentation reaches for react-resizable, and docs/requirements.md forbids substituting a third-party package. So use-column-resize.ts (95 lines) plus a grip rendered through the documented components.header.cell extension point is ours. Keyboard operation is included deliberately: a pointer-only grip would be a WCAG 2.1.1 failure presented as a met requirement, and React Aria's native equivalent is keyboard-operable. This is the same gap Mantine had.",
      "customLinesOfCode": 95
    },
    {
      "id": "table-states",
      "status": "native",
      "notes": "loading on Table renders its own overlay; empty comes from locale.emptyText. Note antd keeps previous rows visible under a dimming overlay while loading rather than clearing them, so the fixture contract rather than antd decides what a loading table contains.",
      "customLinesOfCode": 0
    },
    {
      "id": "form-states",
      "status": "native",
      "notes": "Alert for error and success, Skeleton for loading. server-rejected is driven from Form onFinish into a field-level validateStatus - a real library channel, unlike MUI, React Aria, Carbon and Mantine, all of which had to model it as component state.",
      "customLinesOfCode": 0
    },
    {
      "id": "locale-switcher",
      "status": "native",
      "notes": "Segmented drives the fixture locale; ConfigProvider takes antd's own ar_EG, fr_FR, de_DE and en_GB packs and dayjs.locale() drives the pickers. TRAP for tests: Segmented renders a visually-hidden radio inside a label, so clicking the radio times out even though the role is exposed correctly.",
      "customLinesOfCode": 0
    },
    {
      "id": "rtl",
      "status": "native",
      "notes": "RECORDED AS NATIVE, where every other candidate recorded `composed`. ConfigProvider direction=\"rtl\" is a single documented prop and antd flips its own components from it - no build plugin, no per-component work, no logical-property rewriting of ours. Asserted: .ant-form-rtl, .ant-input-rtl, .ant-table-rtl and .ant-btn-rtl all present. This is the requirement MUI Community cannot meet at all: it needs stylis-plugin-rtl, which constraint 2 forbids, and its floating labels stay pinned to the physical left by up to 870px.",
      "customLinesOfCode": 0
    },
    {
      "id": "long-labels",
      "status": "native",
      "notes": "0px horizontal overflow in German at all three viewports. scroll={{x: \"max-content\"}} on the table keeps the 250-row grid from propagating its intrinsic width up to the document.",
      "customLinesOfCode": 0
    },
    {
      "id": "side-by-side",
      "status": "composed",
      "notes": "Host markup in Mangrove's own classes (mg-button mg-button-primary, mg-table mg-table--striped, mg-card with mg-card__content) beside antd themed to the UNDRR tokens. All six classes verified present in the published 1.8.1 stylesheet before use, because an earlier run of this evaluation invented mg- classes that do not exist. Composed rather than native, matching the other runs: the section is assembled from several components rather than provided by one.",
      "customLinesOfCode": 0
    }
  ],
  "customCss": {
    "lines": 46,
    "selectors": 6,
    "overridesLibraryInternals": false,
    "notes": "No selector reaches an antd class name. The stylesheet is the resize grip antd has no component for, the subtree background reset, and section 9's grid. NOTE what is NOT here: zero rules undoing Mangrove's element-level input, label and legend styling, where the mangrove-mui run needed 4 rules across 16 lines to fight exactly those rules. antd's @layer containment means there is nothing to fight - the host simply wins."
  },
  "wrappers": {
    "count": 4,
    "totalLines": 128,
    "notes": "useColumnResize, the resizable header cell, and the three Select variants in section 2. None of them wrap an antd component to change its appearance; they add behaviour antd does not have or bind props it does not default well."
  },
  "theming": {
    "tokensApplied": 44,
    "tokensUnreachable": 0,
    "method": "A ConfigProvider theme object of SEED tokens, from which antd derives roughly 100 alias tokens. That is why 71 UNDRR tokens map onto far fewer antd inputs without loss: setting colorPrimary produces the hover, active, border and background variants that MUI and Mantine each need told separately. Like MUI's createTheme this is a JavaScript object resolved at bundle time, so a token change means rebuilding every consuming site - antd 6 does have a cssVar mode that would make it live, but enabling it emits antd's own custom properties on a :root-level selector and would put antd's palette in the same global scope as the host, so it was deliberately left off and the trade recorded. On this host a further caveat applies: Mangrove 1.8.1's compiled stylesheet declares zero custom properties (docs/host-derivation.md finding 1), so its palette is unreachable at runtime and antd could not be themed against Mangrove's own values even if the layer order allowed it.",
    "escapeHatchesUsed": [
      "antd/dist/reset.css IS NOT IMPORTED. It writes to html, body and *, and MEASURED it changes 9 watched properties across 3 of 14 host canaries, rewriting heading fonts, weights and margins on the host - see leakage.globalStylesheetProbe. An earlier draft of this file claimed antd 'needs the reset less' than MUI's CssBaseline or Carbon's prebuilt stylesheet; that was asserted before it was measured and is only half right. What IS supported: antd renders correctly WITHOUT the reset, because @ant-design/cssinjs emits self-contained component styles - all 39 e2e assertions pass and axe reports zero critical violations with it omitted. What is NOT supported is any claim that the reset is gentler than the others: it damages every canary on Delta. It is smaller than Carbon's (79 differences) but not harmless.",
      "StyleProvider layer is enabled, which wraps every antd rule in a CSS @layer. This is antd's own containment mechanism and the only first-class one among the five candidates. It has a consequence the other hosts make visible - see the leakage and hostRepair notes.",
      "ConfigProvider getPopupContainer mounts overlays inside the candidate subtree. Without it antd portals to document.body and every var(--undrr-*) in an overlay resolves to nothing, because the tokens are scoped to .undrr-tokens rather than :root.",
      "FINDING, not a preference: antd derives its secondary, description, placeholder and label greys from colorTextBase by lowering opacity, and that derivation produced FOUR axe colour-contrast failures from a palette whose own secondary text passes at 7.3:1. colorTextDescription, colorTextPlaceholder, colorTextHeading and colorTextLabel had to be pinned back to the token values by hand. colorTextSecondary and colorTextTertiary are not in antd's alias interface at all and cannot be set by name. So 'set the seeds and everything follows' is true for hue and false for contrast.",
      "Menu.itemSelectedColor and itemSelectedBg pinned for the same reason: the derived selected-item colour did not reach 4.5:1 against antd's own selected background.",
      "Tag colours come from the UNDRR tokens rather than antd's green/gold/red presets, which failed contrast as filled tags and would have been antd's hues rather than UNDRR's in any case.",
      "Form.Item label does NOT associate the label with its control unless the item also has a name, because that is what antd uses to generate the id it points for at. Rendering fixture states means there is no name to give, so every field needs an explicit htmlFor plus a matching id. Without it axe reports a CRITICAL label violation on every input, which is what the first run of section 1 did."
    ]
  },
  "leakage": {
    "assertionPassed": true,
    "differences": [],
    "notes": "Clean across all 14 canaries and 27 watched properties. antd's @layer containment makes this stronger than a pass alone suggests: unlayered CSS beats layered CSS regardless of specificity, so antd cannot win a conflict against the host even where its selectors are more specific. Note the KNOWN LIMITATION in packages/test-harness/src/leakage.ts still applies - statically imported stylesheets are invisible to this check - but antd ships no static stylesheet here at all, since reset.css is omitted and everything else is runtime-injected.",
    "globalStylesheetProbe": {
      "stylesheet": "antd/dist/reset.css",
      "bytes": 3587,
      "differenceCount": 9,
      "canariesAffected": 3,
      "canariesChecked": 14,
      "verdict": "the documented global reset is NOT loadable as-is",
      "measuredBy": "scripts/probe-global-reset.mjs; raw result in test-results/global-reset-probe.json",
      "notes": "Injecting antd's documented reset changes 9 watched computed properties across 3 of 14 host canaries - it rewrites heading font-family, font-weight and margins on the HOST. That is why it is omitted. The two hosts differ (40 differences on 14 canaries for Delta against 9 on 3 for Mangrove) because Mangrove's own element rules already out-specify most of the reset. For scale: Carbon's prebuilt stylesheet measured 79 differences across all 14."
    }
  },
  "hostRepair": {
    "lines": 0,
    "notes": "ZERO host-repair CSS, for the opposite reason to delta-antd, and this is the most consequential finding of the antd pairing. Mangrove 1.8.1's published stylesheet contains NO @layer at all (verified: zero occurrences), while StyleProvider layer puts every antd rule inside one. Unlayered CSS beats layered CSS regardless of specificity, so Mangrove's input[type=text]{border:2px solid #1a1a1a;height:46px;border-radius:0;font-family:Roboto} wins outright over antd's .ant-input. Measured on a section-1 field: 2px, #1a1a1a, 46px, 0 radius, Roboto - identical to a bare Mangrove input injected into the same page for comparison. The mangrove-mui run needed 27 lines of demo.css to neutralise those same rules and keep MUI looking like MUI. Here nothing was written, and the control looks like the design system. That is either the point or the problem, depending on what UNDRR wants."
  },
  "axe": {
    "violations": 1,
    "critical": 0,
    "serious": 1,
    "incomplete": 1,
    "notes": "The one remaining violation is antd's, not ours: aria-hidden-focus on .ant-table-measure-row. rc-table renders that row with aria-hidden=\"true\" whenever scroll.x is set, and rowSelection puts a focusable checkbox inside it - verified in the DOM: aria-hidden=\"true\", 2 focusable descendants. rowSelection plus a horizontally scrolling table is an ordinary combination, so this will affect any real UNDRR table. Two CRITICAL violations were found and fixed first, and both were ours: a missing label association on every Form.Item, and a focusable role=\"separator\" without aria-valuenow. 4 violations went to 1. Whole-page axe reports 2 violations rather than 1: the extra is Mangrove's own baseline link rule, a known host violation recorded in docs/requirements.md, not antd's."
  },
  "rtl": {
    "status": "clean",
    "issues": []
  },
  "longLabels": {
    "status": "clean",
    "issues": []
  },
  "bundle": {
    "gzippedKb": 423.4,
    "dependencyCount": 69,
    "notes": "388.0 kB gz JS plus 35.5 kB gz CSS - the CSS is larger than delta-antd's because Mangrove's full published stylesheet is bundled. dependencyCount is the production tree measured by scripts/count-dependencies.mjs, which counts all ten apps by one method. 68 of 69 packages are MIT; the one Apache-2.0 entry is the Mangrove package itself. No font licences, and nothing equivalent to the IBM telemetry found in 21 Carbon and Plex packages."
  },
  "buildTimeSeconds": 1.5,
  "blockers": [],
  "humanReviewRequired": [
    "THE CENTRAL QUESTION, and it is a design decision rather than a defect. On this host antd's controls render as MANGROVE's controls, because antd's styles are layered and Mangrove's are not. It cost zero lines of repair CSS, where mangrove-mui needed 27 lines to achieve the opposite. If UNDRR wants Mangrove's look and feel to win by default across many sites, this is the best mechanism found anywhere in this evaluation - it is a single prop, it needs no per-component work, and it cannot drift. If UNDRR wants the UNDRR token mapping to be authoritative over the host, it is a problem, because controlHeight and borderRadius do not reach any control Mangrove styles by element. The choice is reversible per-site by dropping `layer`, which is why it belongs to UNDRR rather than to this evaluation.",
    "Because the host wins, the two antd pairings do NOT look alike, and that is expected rather than a bug. Compare apps/delta-antd/screenshots with apps/mangrove-antd/screenshots at the same viewport to see what `layer` does. Any conclusion drawn from one host's screenshots does not transfer to the other.",
    "If Mangrove 2.0 adopts cascade layers, this behaviour inverts and antd would start winning conflicts against the host. That should be checked against the 2.0 branch before either decision is locked in. Tracked in issue #4.",
    "antd is themed at bundle time, so a Mangrove token change requires rebuilding every consuming site. cssVar mode would fix that but breaks containment as configured here. Whether that trade can be reopened depends on whether :root-level antd custom properties are acceptable once Mangrove 2.0 ships its own.",
    "The aria-hidden-focus defect on .ant-table-measure-row is upstream and unfixable through the public API without giving up scroll.x or row selection. It should be reported to antd.",
    "antd's derived greys failed contrast in four places. They were pinned, but the same derivation will apply to any component whose colour is not explicitly set, so a real deployment needs an audit rather than trusting the seed tokens.",
    "Typography.Link does not underline by default, which is a WCAG 1.4.1 question for inline links in body text. Not fixed here because the same question applies to Mangrove's own link styling and the two should be settled together.",
    "Column resize is ours, so it is ours to maintain: 95 lines including RTL direction handling and keyboard support. Mantine has the same gap. React Aria, MUI and Carbon do not.",
    "demo-state.ts is byte-identical to the copy in integration-mui. It is candidate-independent as well as host-independent and belongs in the scaffold, but packages/ is import-only for demo runs so it was left duplicated rather than churning six other apps."
  ]
}
