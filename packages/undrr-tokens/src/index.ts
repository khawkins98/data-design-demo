/**
 * Neutral design tokens for the UNDRR data design system evaluation.
 *
 * Consumers import `./tokens.css` for the custom properties and this module for
 * the same values in TypeScript. Import only; Brief 1 forbids modification.
 */

export {
  TOKEN_COUNT,
  TOKEN_PREFIX,
  TOKEN_SCOPE_CLASS,
  color,
  cssVarName,
  fontFamily,
  fontSize,
  fontWeight,
  lineHeight,
  radius,
  space,
  tokens,
  tokensAsCssVars,
  zIndex,
} from "./tokens.js";

export type { Tokens } from "./tokens.js";
