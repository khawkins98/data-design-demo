{
  "name": "@undrr-eval/integration-antd",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "The host-independent part of the Ant Design integration, shared by both host apps. Built this way from the start, so axis A3 is answered by construction rather than by a later refactor.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build"
  },
  "dependencies": {
    "@undrr-eval/fixtures": "workspace:*",
    "@undrr-eval/undrr-tokens": "workspace:*"
  },
  "peerDependencies": {
    "antd": "^6.5.3",
    "dayjs": "^1.11.11",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
