{
  "name": "@undrr-eval/fixtures",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "Shared fixture data and labels. Import only; never modify during a demo run.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "generate": "node ../../scripts/generate-fixtures.mjs"
  }
}
